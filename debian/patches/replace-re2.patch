Description: re2 is a drop-in replacement to python's standard re. Since re2
 is missing from Debian archive, we need to replace it in order to make the
 code work.
Author: Mo Zhou <lumin@debian.org>
Forwarded: do not forward.
Index: onnx/onnx/reference/ops/op_regex_full_match.py
===================================================================
--- onnx.orig/onnx/reference/ops/op_regex_full_match.py	2025-01-20 16:03:11.347307755 +0000
+++ onnx/onnx/reference/ops/op_regex_full_match.py	2025-01-20 17:40:59.452994291 +0000
@@ -13,18 +13,18 @@
 class RegexFullMatch(OpRun):
     def _run(self, x, pattern=None):
         try:
-            import re2
+            import re
         except ImportError as e:
             raise ImportError(
-                "re2 must be installed to use the reference implementation of the RegexFullMatch operator"
+                "re must be installed to use the reference implementation of the RegexFullMatch operator"
             ) from e
 
             # As per onnx/mapping.py, object numpy dtype corresponds to TensorProto.STRING
         if x.dtype.kind not in _acceptable_str_dtypes:
             raise TypeError(f"Input must be string tensor, received dtype {x.dtype}")
         try:
-            regex = re2.compile(pattern)
-        except re2.error as e:
+            regex = re.compile(pattern)
+        except re.error as e:
             raise ValueError(f"Invalid regex pattern {pattern!r}") from e
 
         fullmatch_func = np.vectorize(
